package reading

import (
	// "fmt"
	"testing"
)



func TestNewDayReading(t *testing.T) {
	// 测试用例
	// rawInfo := "《三毛流浪记》,30min"
	// readObj, err := NewDayReading(rawInfo)
	// if err != nil {
	// 	t.Errorf("Unexpected error: %s", err)
	// }
	// fmt.Println(readObj.DayRawInfo, readObj.DayReadingContent, readObj.DayReadingTime)

}